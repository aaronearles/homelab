# include "/etc/bind/named.conf.key";

# controls {
#     inet 127.0.0.1 port 953
#     allow { 127.0.0.1; } keys { "tsig-key"; };
# };

acl internal {
  172.20.0.0/14;
};

options {
  forwarders {
    1.1.1.1;
    1.0.0.1;
  };
  allow-query { internal };
};

zone "earles.internal" IN {
  type master;
  file "/etc/bind/earles.internal.zone";
  # update-policy { grant tsig-key zonesub any; };
};

zone "earles.io" IN {
  type master;
  file "/etc/bind/earles.io.zone";
  # update-policy { grant tsig-key zonesub any; };
};
